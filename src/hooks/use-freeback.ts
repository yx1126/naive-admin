import { useLoadingBar, useDialog, useMessage } from "naive-ui";

export const freebackMap = {
    loadingBar: useLoadingBar,
    dialog: useDialog,
    message: useMessage,
};

// export default (type: string) => {};
